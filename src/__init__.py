"""Compatibility package kept for backwards import paths.

The library is now distributed as ``oet_core``. Importing the legacy
``src`` package continues to work but will be removed in a future
release.
"""

from oet_core import *  # noqa: F401,F403
